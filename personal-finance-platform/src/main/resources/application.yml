spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_app
    username: JPAProject
    password: Shanu
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
